branches:
  - main
  - name: develop
    channel: beta
    prerelease: beta
  - name: "feature/*"
    channel: alpha
    prerelease: alpha
plugins:
  - "@semantic-release/commit-analyzer"
  # - "@semantic-release/release-notes-generator"
  - "@semantic-release/github"
tagFormat: "v${version}"
